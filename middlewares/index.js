const validation = require("./validation");
const controllerWrapper = require("./cntrlWrapper");
const auth = require("./auth");
const upload = require("./upload");

module.exports = {
  validation,
  controllerWrapper,
  auth,
  upload,
};
